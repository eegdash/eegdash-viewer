node_modules/
playwright-report/
test-results/
scripts/.cache/
scripts/cross_check_*.json
perf-trace.json
.DS_Store
coverage/
.nyc_output/
.stryker-tmp/
reports/mutation/
# tinybench raw outputs — CI artifacts, not source of truth.
bench/results-*.json
bench/results-all-gab.json
reports/stryker-incremental.json
# Stryker incremental cache grew to 54 MB after iter-8 scope expansion.
# Local-only now; nightly CI rebuilds cold (~9 min). Saves repo bloat.
